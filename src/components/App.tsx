import * as React from 'react';
import { StatelessComponent } from 'react';
import './App.css';

const App: StatelessComponent<{}> = props => (
  <div className="App">
    <p>Initialised</p>
  </div>
);

export default App;
