import * as React from 'react';
import { StatelessComponent } from 'react';
import './index.css';

const Sidebar: StatelessComponent<{}> = props => (
  <aside className='Sidebar'>Sidebar</aside>
);

export default Sidebar;
